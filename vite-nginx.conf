server {
    listen 80;

    location /gratitude-sparkle {
        alias /usr/share/nginx/html/gratitude-sparkle;
        index index.html index.htm;
        try_files $uri $uri/ /gratitude-sparkle/index.html;
    }

    location = / {
        return 301 /gratitude-sparkle/;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|otf)$ {
        root /usr/share/nginx/html;
        expires 1y;
        add_header Cache-Control "public";
    }
}